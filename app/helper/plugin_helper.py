class PluginHelper:

    @staticmethod
    def install(plugin_id):
        """
        插件安装统计计数
        远程服务 nastool.org 已失效并被劫持，禁用遥测上报。
        """
        return None

    @staticmethod
    def report(plugins):
        """
        批量上报插件安装统计数据
        远程服务 nastool.org 已失效并被劫持，禁用遥测上报。
        """
        return None

    @staticmethod
    def statistic():
        """
        获取插件安装统计数据
        远程服务 nastool.org 已失效并被劫持，禁用遥测获取。
        """
        return {}
